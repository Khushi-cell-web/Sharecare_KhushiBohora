package com.example.frontendsharecare

import io.flutter.embedding.android.FlutterFragmentActivity

// Stripe Payment Sheet needs FragmentActivity (Support Fragment Manager).
class MainActivity : FlutterFragmentActivity()
